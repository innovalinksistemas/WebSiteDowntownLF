---
import "../../styles/global.css";
import Layout from "../../layouts/Layout.astro";
import Footer from "../../components/Footer.astro";
---

<Layout
  title="Page not found | Downtown La Fortuna Boutique Hotel"
  description="The page you are looking for does not exist or has moved."
  headerSolid={true}
>
  <!-- noindex: una 404 nunca debe indexarse, aunque devuelva 200 en hosts estáticos. -->
  <meta slot="head" name="robots" content="noindex, follow" />

  <main id="contenido" class="nf-main">
    <p class="nf-code">404</p>
    <h1 class="nf-title">This page got lost in the Arenal mist</h1>
    <p class="nf-text">
      The link you followed does not exist or has moved. Head back home, or get
      in touch and we will help you find it.
    </p>
    <div class="nf-actions">
      <a class="nf-btn nf-btn--primary" href="/en/">Go home</a>
      <a class="nf-btn nf-btn--outline" href="/en/arenal-fortuna/">Arenal Fortuna guide</a>
      <a class="nf-btn nf-btn--outline" href="/en/gallery/">Gallery</a>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  .nf-main {
    min-height: 68vh;
    display: grid;
    align-content: center;
    justify-items: center;
    text-align: center;
    gap: 0.75rem;
    padding: 8rem 1.5rem 4rem;
    background: var(--brand-cream, #f5f1eb);
  }
  .nf-code {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(4rem, 12vw, 8rem);
    line-height: 1;
    color: var(--brand-red, #b90024);
  }
  .nf-title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 4vw, 2.6rem);
    font-weight: 500;
    color: var(--brand-black, #1e1e1e);
    max-width: 22ch;
  }
  .nf-text {
    margin: 0;
    max-width: 46ch;
    color: #5a5a5a;
    line-height: 1.6;
  }
  .nf-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
    margin-top: 1rem;
  }
  .nf-btn {
    padding: 0.75rem 1.5rem;
    border-radius: 999px;
    text-decoration: none;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .nf-btn--primary {
    background: var(--brand-red, #b90024);
    color: #fff;
  }
  .nf-btn--outline {
    border: 1px solid rgba(30, 30, 30, 0.25);
    color: var(--brand-black, #1e1e1e);
  }
</style>
